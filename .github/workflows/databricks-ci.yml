name: Upload to Databricks DBFS

on:
  push:
    branches: ["develop"]   # pipeline will run when you push to 'develop'

jobs:
  upload-code:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Databricks
        run: |
          pip install databricks

      - name: Authenticate with Databricks
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          databricks configure --host "$DATABRICKS_HOST" --token "$DATABRICKS_TOKEN" --quiet

      - name: Upload scripts to DBFS
        run: |
          databricks fs mkdirs dbfs:/FileStore/code/portfolio/
          databricks fs cp --overwrite --recursive databricks/src/ dbfs:/FileStore/code/portfolio/src/
